import { Button } from "react-bootstrap";
import "./BookDrivingBtn.css";
const BookDrivingBtn = () => {
  return (
    <Button className="text-uppercase btn book-driving-btn">
      Book Driving Lesson
    </Button>
  );
};

export default BookDrivingBtn;
